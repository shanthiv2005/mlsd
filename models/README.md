# This folder stores trained model artifacts.
# model.pkl, scaler.pkl, and threshold.txt are generated by src/train.py
# and are git-ignored by default.
# To deploy to Streamlit Cloud, remove models/ from .gitignore and commit them.
